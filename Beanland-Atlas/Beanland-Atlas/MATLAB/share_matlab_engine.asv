function [ success ] = share_matlab_engine( name )
%Name and share a MATLAB engine

%check if the engine is already shared
shared = matlab.engine.isEngineShared(name);

if !shared
matlab.engine.shareEngine(name);
success = 1;
end